{
  "name": "lambda-typescript",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "create": "sls create --template aws-nodejs",
    "deploy": "sls deploy",
    "tsc": "tsc --noEmit"
  },
  "devDependencies": {
    "@types/node": "^14.14.20",
    "typescript": "^4.1.3"
  }
}
